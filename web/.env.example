# Supabase配置
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
VUE_APP_SUPABASE_URL=$NEXT_PUBLIC_SUPABASE_URL
VUE_APP_SUPABASE_ANON_KEY=$NEXT_PUBLIC_SUPABASE_ANON_KEY

# 本地开发配置
VUE_APP_SUPABASE_ENABLED=false

# 使用说明：
# 1. 在 Supabase 控制台获取项目 URL 和 anon key
# 2. 将值替换上面的占位符
# 3. 在生产环境中将 VUE_APP_SUPABASE_ENABLED 设置为 true
# 4. NEXT_PUBLIC_* 格式是 Supabase 推荐的环境变量格式